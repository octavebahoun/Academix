[phases.setup]
nixPkgs = [
  "python312",
  "ffmpeg",
  "cairo",
  "pango",
  "gdk-pixbuf",
  "glib",
  "libffi",
  "harfbuzz",
  "fontconfig"
]

[phases.build]
cmds = [
  "python -m venv .venv && .venv/bin/pip install --upgrade pip && .venv/bin/pip install -r requirements.txt"
]
