providers = ["python"]

[phases.setup]
nixPkgs = [
  "python311",
  "ffmpeg",
  "cairo",
  "pango",
  "gdk-pixbuf",
  "glib",
  "libffi",
  "harfbuzz",
  "fontconfig"
]

[phases.build]
# Le provider python s'occupe déjà de "pip install -r requirements.txt"
# On n'a rien besoin d'ajouter ici, Railway le fera auto-magiquement !
